class allict (
        $host = "winlabels.local",        # Hostname to listen to
        $port = 80,                         # Actual port
        $phpmyadmin = true,                 # Do we need to install phpmyadmin or not
        $xdebug = true,                     # Do we need to install xdebug?

        $dbhost = "localhost",              # Database settings
        $dbname = "campaigntracker",
        $dbuser = "root",
        $dbpass = "",
    ) {


}
